[project]
name = "pubmed-rag-backend"
version = "0.1.0"
description = "PubMed RAG system backend"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "openai>=1.0",
    "pymilvus>=2.3",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
